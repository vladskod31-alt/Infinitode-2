plugins {
    id 'com.android.application'
}

android {
    namespace 'com.infinitode4.game'
    compileSdk 34

    defaultConfig {
        applicationId 'com.infinitode4.game'
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName '1.0.0'
    }

    signingConfigs {
        release {
            storeFile rootProject.file('infinitode4-release.p12')
            storePassword INF4_STORE_PASSWORD
            keyAlias INF4_KEY_ALIAS
            keyPassword INF4_KEY_PASSWORD
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            shrinkResources false
        }
        debug {
            applicationIdSuffix '.debug'
        }
    }

    // Bundle the HTML5 game as WebView assets
    sourceSets {
        main {
            assets.srcDirs += ['../../game']
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
